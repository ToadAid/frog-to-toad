{
  "stage": 0,
  "name": "Eyes",
  "runnable": {
    "read": ["market_price"],
    "write": ["journal_append", "memory_save"]
  },
  "reserved": {
    "read": ["market_token_search", "market_technicals", "market_news", "market_sentiment", "market_trending", "market_onchain"],
    "write": [],
    "note": "RESERVED / NOT YET WIRED — advertised nowhere, refused fail-closed if requested"
  },
  "denied": [
    "swap_execute", "paper_bracket_handoff", "paper_perp_open", "sniper_scan", "sniper_shadow_probe",
    "send_alert", "schedule_create", "spawn_subagent", "spawn_scouts", "convene_witness",
    "mailbox_send", "github write tools", "wallet tools"
  ],
  "notes": "Runnable = implemented = advertised (runtime refuses fail-closed on roster drift). Write scope: own stores only (agent1/stores/). memory_save provenance is structural: UNVERIFIED_WORKING_NOTE only; PRINCIPAL_DECLARED requires an authenticated principal path that does not exist in Stage 0."
}